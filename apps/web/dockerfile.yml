# 🧱 Dockerfile para o Frontend Web (React + TanStack Router + Vite)
FROM node:22-alpine AS dev

# Instala pnpm
RUN corepack enable && corepack prepare pnpm@9.0.0 --activate

WORKDIR /workspace

# Copia tudo
COPY . .

# Define o comando padrão
CMD ["pnpm", "--filter", "@apps/web...", "dev"]
